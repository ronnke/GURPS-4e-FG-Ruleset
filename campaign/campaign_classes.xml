<?xml version="1.0" encoding="iso-8859-1"?>

<!-- 
  Please see the license.html file included with this distribution for 
  attribution and copyright information.
-->

<root>

  <windowclass name="char_section_modifiers">
		<script>
			function onInit()
        self.onLockModeChanged(WindowManager.getWindowReadOnlyState(self));
      end
      function onLockModeChanged(bReadOnly)
        WindowManager.callSafeControlsSetLockMode(self, { "list_iadd" }, bReadOnly);
      end
			function onDrop(x, y, draginfo)
--				return list.onDrop(x, y, draginfo);
			end			
		</script>
    <sheetdata>
      <anchor_content_top />

      <header_content_framed_headersimple name="header">
        <anchored>
          <left offset="-5" />
          <right offset="5" />
        </anchored>
        <static textres="char_label_modifier" />
      </header_content_framed_headersimple>

      <button_iadd name="list_iadd">
        <anchored to="header" position="insidetopright" offset="5,0" />
        <target>list</target>
      </button_iadd>

      <list_content_noscroll_alternating_top name="list">
        <datasource>.modifierlist</datasource>
        <class>basic_modifier</class>
        <sortby><control>name</control></sortby>
        <newfocus>name</newfocus>
      </list_content_noscroll_alternating_top>

    </sheetdata>
	</windowclass>

	<windowclass name="basic_modifier">
    <margins control="0,0,0,2" />
    <script>
      function onInit()
        self.onLockModeChanged(WindowManager.getWindowReadOnlyState(self));
      end
      function onLockModeChanged(bReadOnly)
        WindowManager.callSafeControlsSetLockMode(self, { "name", "modifier", "idelete" }, bReadOnly);
      end
			function actionDrag(draginfo)
				if not name.isEmpty() then
					draginfo.setType("number");
					draginfo.setDescription(name.getValue());
					draginfo.setStringData(name.getValue());
					draginfo.setNumberData(modifier.getValue());
				end
				return true;
			end
			function action()
				ModifierStack.addSlot(name.getValue(), modifier.getValue());
				return true;
			end
		</script>
		<sheetdata>
      <anchor_listitem_left_sm />
      <anchor_listitem_right_sm />

      <button_listitem_idelete_left name="idelete" />

      <button_listitem_right name="button_modifier">
		    <script>
          function onDragStart(button, x, y, draginfo)
						return window.actionDrag(draginfo);
					end

					function onButtonPress()
						return window.action();
					end
		    </script>
        <anchored width="20" height="20" />
        <icon normal="button_action_modifier" pressed="button_action_modifier_down" />
        <tabtarget prev="modifier" />
      </button_listitem_right>

      <number_listitem_right name="modifier">
        <anchored width="30" />
        <frame mergerule="replace" name="fielddark" offset="5,5,5,5" hidereadonly="true" />
        <displaysign />
        <tabtarget next="button_modifier" prev="name" />
      </number_listitem_right>

      <string_listitem_center name="name" >
        <tabtarget next="modifier" />
      </string_listitem_center>
		
	  </sheetdata>
	</windowclass>
	
</root>